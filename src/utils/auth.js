// Auth functionality goes here to keep Vuex store Auth System agnostic

// import AUTH_CONFIG from './authConfig'

export const sigupNewUser = async ({ user }) => {
  /* ...Specific functionality for auth system */
  console.log('user==>>', user)
}
